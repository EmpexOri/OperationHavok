# Godot 4+ specific ignores
.godot/                     # Godot project metadata (workspace and editor settings)
.nomedia                    # Folders marked as containing no media

# Godot-specific ignores
.import/                    # Folder for imported assets (textures, models, etc.)
export.cfg                  # Export settings for the project
export_credentials.cfg      # Export credentials for the project

# Imported translations (automatically generated from CSV files)
*.translation               # Translation files automatically generated by Godot

# Mono-specific ignores (only if using C#)
.mono/                      # Folder containing Mono-specific data and project files
data_*/                     # Mono-related data
mono_crash.*.json           # Mono crash logs and reports

# Temporary and cache files
.metadata/                  # Godot-specific metadata files
.tmp/                       # Temporary files generated by Godot (e.g., backups)
.cache/                     # Cache folder for editor and project data

# User-specific files
*.godot_user                # User-specific project configuration (editor preferences)
.editorconfig               # EditorConfig settings (optional, if you use one)
*.swp                       # Vim swap files

# Build and export artifacts
*.pck                       # Godot packaged game files
*.exe                       # Windows executables
*.app                       # macOS applications
*.apk                       # Android packages
*.ios                       # iOS project files
*.html                      # HTML5 export files

# Logs and backups
*.log                       # Log files from Godot or the OS
*.bak                       # Backup files
*.tmp                       # Temporary files

# Crash dumps and debugging
crash_dump/                 # Crash dump files generated by Godot
